# Baked into the static export at build time. Without these, `next build`
# falls back to the localhost defaults in lib/api.ts and the deployed panel
# points at a machine that isn't there.
NEXT_PUBLIC_API_URL=https://panel.sebkucera.dev
NEXT_PUBLIC_WS_URL=wss://panel.sebkucera.dev
